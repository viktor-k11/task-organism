# Shipped by Lens Studio

# The lines below are automatically generated and updated by Lens Studio.
# Please do not modify them manually, as changes will be overwritten each time!
# If you want to make changes to this file, please put them either above or below this section.

Cache/**
PluginsUserPreferences/**
Support/**
BackUp/**
Workspaces/**
.vscode/**
/jsconfig.json
/tsconfig.json
.DS_Store
/spk_debug_key.pem
*.lock

# Shipped by Lens Studio
# BEGIN Lens Studio managed Agents-Docs block
.mcp.json
.cursor/mcp.json
.vscode/mcp.json
.codex/config.toml
.claude/settings.local.json
# END Lens Studio managed Agents-Docs block
*.lock

# Added by Lens Studio (LEAF plugin)
leaf_plugin_settings.json

# Asset generator scratch (one-shot regenerator scripts for /build-sfx)
tempAssetGen/

# Perfetto captures — large binaries, analysis lives in prompts.md
performance_traces/

# Build-gate evidence — per-machine capture output, not a shared artifact.
# The gate judges freshness by mtime, which does not survive a clone anyway.
.build-gate/

# Untracked Lens Studio packages — verified NOT required to build.
# Determined empirically: with all three removed (and the parked
# TaskUnderstandingProbe removed, which a fresh clone also lacks), the project
# compiles with zero errors outside Cache/, which is itself gitignored.
#   RemoteServiceGateway — needed ONLY by Assets/Scripts/Debug/TaskUnderstandingProbe.ts,
#                          an untracked parked AI experiment. Install from the
#                          Asset Library if that work is resumed.
#   SnapDecorators       — referenced by nothing in Assets/ or in the SIK,
#   Utilities              UIKit and Leaf archives (checked inside the .lspkg zips).
Packages/RemoteServiceGateway.lspkg
Packages/RemoteServiceGateway.lspkg.meta
Packages/SnapDecorators.lspkg
Packages/SnapDecorators.lspkg.meta
Packages/Utilities.lspkg
Packages/Utilities.lspkg.meta

# Per-machine Lens Studio plugin state, keyed by a local install UUID.
Plugins/
