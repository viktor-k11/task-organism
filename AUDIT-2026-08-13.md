# Audit — 2026-08-13 (Thursday, day 4 of 7)

Read-only audit. No files were modified, staged, committed or pushed; this
report is the only file created. Deadline: **Sunday 2026-08-16 18:00 Prague**
— roughly **3.5 working days** remain.

Every claim below is backed by command output, file contents, log lines or
runtime queries collected today. Anything not verified is marked
**UNVERIFIED** with the action that would settle it.

Lens Studio **was** running with this project loaded
(`ExecuteEditorCode` → `/Users/kateryna/Desktop/hackaton/task-organism/task-organism.esproj`),
so sections 4 and 8 are real measurements, not assumptions.

---

## 1. Repository state

```
4e659db 2026-08-12 day3: sfx variants, release cue timing, habitat controls, vertex shading, grounding fix
4e6e6c1 2026-08-12 docs: consolidate prompts.md into a complete CLAD process log
a83b968 2026-08-12 feat: presentation hardening - shading, grounding, motion, story, staging
65215c2 2026-08-11 feat: swap creature visual to ready-made dog/cat models
381a0f1 2026-08-11 docs: additive display investigation and recording constraint
d267065 2026-08-11 docs: handoff, agent instructions, meta sync
de54856 2026-08-11 docs: prepare Claude handoff
bcfe295 2026-08-11 fix: clarify vertical-slice presentation
46db5dd 2026-08-11 feat: complete judge-facing vertical slice
b6a9e1b 2026-08-11 feat: complete gate 3 interaction loop
7989312 2026-08-11 feat: verify gate 2 product truth
0ed92b2 2026-08-11 feat: add versioned task data layer
054cd4b 2026-08-11 feat: freeze technical character checkpoint
99521a3 2026-08-11 feat: redesign blob as living creature
7e81260 2026-08-11 test: verify emotional prototype vitality
aa63d58 2026-08-11 feat: debug-only habitat recenter for preview/recording
1b95977 2026-08-11 chore: day-1 adjustments - wobble perf gate, defer sound/LEAF
0a353d2 2026-08-11 feat: emotional core - procedural blob creature (idle/chase/release)
fce0f97 2026-08-11 chore: ignore lens studio lock files
116be35 2026-08-11 init: SPECS project with CLAUDE.md project rules
```
(20 commits total — the full history, not a truncation at 25.)

| Item | Value | Evidence |
|---|---|---|
| Branch | `main`, tracking `origin/main` | `git branch -vv` |
| Remote | `git@github.com:viktor-k11/task-organism.git` | `git remote -v` |
| Local vs origin | **0 ahead / 0 behind — fully pushed** | `git rev-list --left-right --count origin/main...HEAD` → `0  0` |
| Last commit | `4e659db`, 2026-08-12 23:35:43 +0200 | `git log -1` |
| Last push | 2026-08-12 23:35:55 +0200 | `git reflog show origin/main` |
| Repo public? | **YES — public** | `curl -s -o /dev/null -w "%{http_code}" https://api.github.com/repos/viktor-k11/task-organism` → **HTTP 200** unauthenticated. `gh` CLI is not installed, so the API was used instead. |

**Working tree** (`git status --porcelain=v1`):
```
 M .virtual-scene.json      (7408 insertions / 4222 deletions — VirtualScene inspection artifact)
?? jsconfig 3.json          (Lens Studio duplicate; jsconfig.json + "jsconfig 2.json" also present)
```
Nothing staged (`git diff --cached --stat` empty). Both are deliberately
excluded per `HANDOFF.md`'s "do not stage inspection artifacts" rule.

**Commit cadence — the headline problem:**

| Day | Commits |
|---|---|
| 2026-08-11 (Mon/day 2) | 17 |
| 2026-08-12 (day 3) | 3 |
| **2026-08-13 (today, day 4)** | **0** |

There is **no commit gap inside the history**, but there is a **21-hour gap
from the last commit to now**, and day 4 has produced nothing yet. Per the
frozen playbook, Thursday is `polish → ВОРОТА 4 → FEATURE FREEZE → publish
first candidate → start collecting clips`. None of that has begun.

**Security check (clean):** `spk_debug_key.pem` (a `BEGIN PRIVATE KEY`
signing key) sits in the project root. It is **gitignored** (`.gitignore:16`)
and **never committed** (`git log --all -- spk_debug_key.pem` empty). No leak
in a public repo. Do not remove the ignore rule.

**Repo weight:** `.git` is 51 MB. `Assets/**` and `Packages/**` are
LFS-tracked (`.gitattributes:9-10`), so judges cloning without `git lfs` get
pointer files instead of the GLBs. **UNVERIFIED:** whether the GitHub repo has
LFS bandwidth/storage enabled for anonymous clones — verify by cloning the
public URL into a temp dir on a machine with `git lfs` and confirming the GLBs
materialise.

---

## 2. Code inventory vs architecture

### Assets/Scripts (4560 lines TypeScript, 33 files)

```
   6  Input/TaskInputSource.ts            682  Config/CreatureConfig.ts
  15  Creature/CreatureShadow.ts         1248  Creature/CreatureBehavior.ts
  17  State/TaskResolutionService.ts      482  Interaction/TaskOrganismController.ts
  21  State/AttentionArbiter.ts           197  Creature/ReleaseEffect.ts
  22  Input/DemoInput.ts                  152  Interaction/DemoControlView.ts
  27  Data/Clock.ts                       145  Debug/CreatureDebugTrigger.ts
  27  Input/KeyboardInput.ts              142  Creature/CreatureMovement.ts
  30  Creature/CreatureAppendages.ts      127  Creature/CreaturePetVisual.ts
  31  Interaction/TaskTextFormatting.ts   123  Interaction/DemoSequence.ts
  34  Data/TaskRecord.ts                  122  Interaction/TaskSelectionView.ts
  38  State/StateEngine.ts                111  Creature/CreatureEyes.ts
  45  Input/TaskCreationService.ts         94  Creature/BlobMeshBuilder.ts
  48  Creature/CreatureMaterials.ts        88  Interaction/HabitatFloor.ts
  58  Data/TaskStorage.ts                  82  Creature/CreatureEarsAndTail.ts
  61  Data/TaskRepository.ts               76  Interaction/CreatureInteractionState.ts
  68  Creature/CreatureMouth.ts            71  Creature/CreatureBody.ts
  70  Creature/LatheGeometry.ts
```

### Assets/Tests (15 files, all dated 2026-08-11 13:32–13:54)
12 registered LEAF scenarios + `Gate2Support.ts`, `Gate3Support.ts`, `LeafIndex.ts`.

### Architecture chain (CLAUDE.md) — every layer exists

| Layer | File | Status | What it actually does |
|---|---|---|---|
| Input | `Input/TaskInputSource.ts`, `DemoInput.ts`, `KeyboardInput.ts` | EXISTS | Two sources both delegating to one creation service |
| Creation | `Input/TaskCreationService.ts` | EXISTS | Assigns id + `appearanceSeed` (hash ^ sequence), defaults |
| Repository | `Data/TaskRepository.ts` | EXISTS | Open-task list, add/snooze/resolve, cap, copy-on-read |
| Storage | `Data/TaskStorage.ts` | EXISTS | `schemaVersion:1` JSON; persistent + memory adapters |
| Clock | `Data/Clock.ts` | EXISTS | `Clock` interface, `RealClock`, `DemoClock` |
| State engine | `State/StateEngine.ts` | EXISTS | `urgency()` = age/window + bonuses; `deriveState()` |
| Arbiter | `State/AttentionArbiter.ts` | EXISTS | Max-urgency eligible task, else `null` |
| Presentation | `Creature/CreatureBehavior.ts` (+ PetVisual, ReleaseEffect, HabitatFloor) | EXISTS | Wander/chase/release, posture, colour, grounding |
| Interaction | `Interaction/CreatureInteractionState.ts` | EXISTS | Role frozen at press: select vs resolve |
| Resolution | `State/TaskResolutionService.ts` | EXISTS | Repository mutation **then** presentation callback |

No layer is missing or partial.

### The single config file — all 155 constants

**Habitat / distances (cm, deg)**
`HABITAT_RADIUS_MIN_CM 100` · `HABITAT_RADIUS_MAX_CM 150` · `HABITAT_ARC_HALF_ANGLE_DEG 60` ·
`HABITAT_VERTICAL_OFFSET_CM -10` · `HABITAT_HOME_DEPTH_CM 240` · `HABITAT_HOME_LATERAL_SPACING_CM 36` ·
`HABITAT_HOME_GROUP_LATERAL_CM 0` · `HABITAT_HOME_SIDE_DEPTH_OFFSET_CM 6` · `HABITAT_HOME_WANDER_RADIUS_CM 3` ·
`EYE_HEIGHT_CM 150` · `GROUND_Y_OFFSET_CM -150` · `HABITAT_DEPTH_STEP_CM 15` · `HABITAT_LATERAL_STEP_CM 15` ·
`HABITAT_DEPTH_MIN_CM 120` · `HABITAT_DEPTH_MAX_CM 420` · `HABITAT_LATERAL_LIMIT_CM 60`

**Chase**
`CHASE_DISTANCE_MIN_CM 110` · `CHASE_DISTANCE_MAX_CM 130` · `CHASE_STOP_DISTANCE_CM 100` ·
`CHASE_SIDE_OFFSET_MIN_DEG 0` · `CHASE_SIDE_OFFSET_MAX_DEG 0` ⚠️ · `CHASE_MAX_ACCEL_CM_S2 90` ·
`CHASE_ARRIVAL_RADIUS_CM 8` · `CHASE_DEAD_ZONE_RADIUS_CM 3` · `CHASE_ANGULAR_ARRIVAL_DEG 5` ·
`CHASE_ANGULAR_DEAD_ZONE_DEG 1.5` · `CHASE_HESITATION_INTERVAL_MIN_S 1.0` / `MAX_S 2.0` ·
`CHASE_HESITATION_ANGLE_DEG 6` · `CHASE_HESITATION_DURATION_S 0.5` · `CHASE_LOOK_PAUSE_S 0.3` ·
`CHASE_ANTICIPATION_S 0.28` · `CHASE_ANTICIPATION_DIP_CM 1.4` · `CHASE_MAX_YAW_SPEED_DEG_S 90`

⚠️ **CLAUDE.md spec drift:** the spatial rule says the chaser must sit at an
**8–12° side offset (NOT 30)**. Both side-offset constants are **0**, i.e. the
chaser approaches dead-centre. Not a crash, but a written spec violated.

**Speeds / motion**
`MAX_SPEED_CM_S 50` · `MAX_STEP_DT_S 1/15` · `WANDER_CALM_SPEED_CM_S 7` · `WANDER_CALM_MAX_ACCEL_CM_S2 16` ·
`WANDER_CALM_RADIUS_SCALE 0` · `WANDER_URGENT_SPEED_CM_S 14` · `WANDER_URGENT_MAX_ACCEL_CM_S2 26` ·
`WANDER_URGENT_RADIUS_CM 16` · `WANDER_ARRIVAL_RADIUS_CM 6` · `WANDER_DEAD_ZONE_RADIUS_CM 2` ·
`WALK_BOB_AMPLITUDE_CM 1.1` · `WALK_BOB_HZ_PER_CM_S 0.093` · `WALK_BOB_FULL_SPEED_CM_S 12` ·
`FACE_TURN_RATE_PER_S 4`

**Timings (s / ms)**
`WANDER_CALM_REPICK_PAUSE_MIN_S 3.5` / `MAX_S 8.0` · `WANDER_URGENT_REPICK_PAUSE_MIN_S 1.2` / `MAX_S 3.0` ·
`SQUASH_STRETCH_DURATION_S 0.35` · `POSTURE_EASE_PER_S 3.0` · `PRESENTATION_SCALE_EASE_PER_S 2.8` ·
`EXPRESSION_EASE_PER_S 4.0` · `TINT_EASE_PER_S 3.0` · `GROWTH_EASE_PER_S 2.0` ·
`BLINK_INTERVAL_MIN_S 2.8` / `MAX_S 5.5` · `BLINK_DURATION_S 0.16` · `RESOLVE_HOLD_DURATION_S 1.5` ·
`LATER_SNOOZE_DURATION_MS 900000` · `RELEASE_DURATION_S 1.5`

**Thresholds**
`URGENCY_AGE_WINDOW_MS 86400000` · `CHASE_THRESHOLD 1` · `HIGH_IMPORTANCE_URGENCY_BONUS 0.5` ·
`SQUASH_STRETCH_DIRECTION_DOT_THRESHOLD 0.4`

**Breathing / posture / gaze**
`BREATHE_CALM_AMPLITUDE 0.018` @ `0.18 Hz` · `BREATHE_URGENT_AMPLITUDE 0.012` @ `1.1 Hz` ·
`BREATHE_CHASE_AMPLITUDE 0.03` @ `0.8 Hz` · `POSTURE_CALM_HEIGHT/WIDTH 0.86 / 1.14` ·
`POSTURE_URGENT 1.18 / 0.90` · `POSTURE_CHASE 1.08 / 0.97` · **`POSTURE_URGENT_TREMOR_AMPLITUDE 0.0`**
(disabled) @ `5.5 Hz` · `GAZE_CALM_YAW_SPEED_DEG_S 18` · `GAZE_CALM_DRIFT_RANGE_DEG 18` @ `0.05 Hz` ·
`GAZE_URGENT_YAW_SPEED_DEG_S 180` · **`GAZE_URGENT_TREMOR_DEG 0`** (disabled) @ `3.2 Hz` ·
`BODY_MOVE_TILT_DEG 8` · `BODY_SECONDARY_SWAY_DEG 2.5` · `SQUASH_STRETCH_AMOUNT 0.18` ·
`WOBBLE_ENABLED true` · `WOBBLE_AMPLITUDE_CM 0.12` @ `0.45 Hz` · `WOBBLE_PHASE_STEP 0.9`

**Colour**
`CREATURE_PALETTE` 6 entries (amber `1.00,0.55,0.08` · cyan `0.10,0.80,0.98` · magenta `1.00,0.24,0.62` ·
green `0.42,0.92,0.26` · violet `0.64,0.40,1.00` · yellow `1.00,0.86,0.12`) ·
`VERTEX_SHADING_AMOUNT 1.0` · `TINT_HEAT_COLOR 1.0,0.26,0.12` · `TINT_URGENT_HEAT_BLEND 0.15` ·
`TINT_CHASE_HEAT_BLEND 0.28` · `BLOB_COLOR 0.75,0.48,0.1` · `EYE_COLOR 0.12,0.1,0.09` ·
`MOUTH_COLOR 0.42,0.20,0.18`

**Scale / geometry**
`HABITAT_VISUAL_SCALE 0.68` · `CHASE_VISUAL_SCALE 0.95` · `GROWTH_SCALE_MAX 1.25` ·
`READYMADE_PET_TARGET_HEIGHT_CM 34` · `READYMADE_PET_HALF_HEIGHT_CM 17` ·
`READYMADE_PET_YAW_CORRECTION_DEG 180` · `DOG_DISPLAY_SCALE ≈16.57` · `CAT_DISPLAY_SCALE ≈40.19` ·
`PET_CREATURE_HALF_HEIGHT_CM 18.5` · `LATHE_SEGMENTS 80` · `VISUAL_BASELINE_SCALE 0.88` ·
`BLOB_PROFILE` (lathe profile table)

**Face (legacy procedural)**
`EYE_RADIUS_CM 3.1` · `EYE_OFFSET_Y_CM 5.5` · `EYE_OFFSET_Z_CM -15.0` · `EYELID_CALM/URGENT/CHASE 0.5/0/0` ·
`EYE_SCALE_CALM/URGENT/CHASE 0.9/1.18/1.25` · `EYE_SPACING_*_CM 5.5/6.5/7.0` ·
`LEAN_*_PITCH_DEG -4/6/9` · `MOUTH_ENABLED true` · `MOUTH_SEGMENT_COUNT 5` · `MOUTH_WIDTH_CM 5.0` ·
`MOUTH_BEAD_RADIUS_CM 1.3` · `MOUTH_OFFSET_Y/Z_CM 2.0/-17.5` · `MOUTH_CURVE_HEIGHT_CM 1.6` ·
`MOUTH_CURVE_CALM/URGENT/CHASE 0.35/-0.45/0.55`

**Demo / staging / UI**
`DEMO_BEAT_CALM_HOLD_S 3.5` · `DEMO_BEAT_URGENT_DWELL_S 5.5` · `DEMO_BEAT_APPROACH_S 4.0` ·
`DEMO_BEAT_SELECT_READ_S 2.0` · `DEMO_BEAT_RESOLVE_SETTLE_S 0.5` · `DEMO_AUTOPLAY_ON_START true` ·
`HABITAT_LABEL_MAX_CHARS 18` · `SELECTION_LINE_MAX_CHARS 30` · `TASK_SELECTION_PANEL_Y_CM 25`

**Release**
`RELEASE_SFX_VARIANT "breath"` (+ type) · `RELEASE_PARTICLE_COUNT 30` · `RELEASE_PARTICLE_SPEED_CM_S 8` ·
`RELEASE_PARTICLE_DRIFT_CM 4` · `RELEASE_PARTICLE_SIZE_CM 0.5` · `RELEASE_BRIGHTEN_LERP 0.5`

### Dead / leftover code

Import-graph scan (`grep -rln "from ...<module>"` excluding self):

| File | Importers | Verdict |
|---|---|---|
| `Creature/BlobMeshBuilder.ts` (94 L) | **0** | **DEAD** — procedural blob, abandoned at the GLB swap |
| `Creature/CreatureBody.ts` (71 L) | **0** | **DEAD** — `PetCreatureBody`, superseded by `CreaturePetVisual` |
| `Creature/CreatureAppendages.ts` (30 L) | **0** | **DEAD** |
| `Creature/CreatureEyes.ts` (111 L) | 1 (`CreatureBehavior`) | Imported but **inert** on the GLB path — `CreatureBehavior` comments state eyes are optional placeholders and the ready-made GLBs have none |
| `Creature/CreatureMouth.ts` (68 L) | 1 | same — inert |
| `Creature/CreatureEarsAndTail.ts` (82 L) | 1 | same — inert |
| `Debug/CreatureDebugTrigger.ts` (145 L) | — | Debug-only auto-cycle harness |

≈**195 lines fully dead**, ≈**261 more imported-but-inert**. Matching config
blocks (`BLOB_PROFILE`, `LATHE_SEGMENTS`, all `EYE_*`/`MOUTH_*`, `WOBBLE_*`,
`PET_CREATURE_HALF_HEIGHT_CM`) are likewise unused by the shipping path.
`Assets/GeneratedMeshes/PetCreature.glb` (1.4 MB) is **not referenced by any
`requireAsset`** — dead payload in an LFS repo.

---

## 3. Invariant compliance (verified by reading code today)

| # | Invariant | Verdict | Evidence |
|---|---|---|---|
| 1 | Behavior state computed, never persisted | **PASS** | `Data/TaskRecord.ts:5-16` has no state field; `Data/TaskStorage.ts:7-10` payload is `{schemaVersion, tasks}` only. `BehaviorState` is a return type of `StateEngine.deriveState` (`State/StateEngine.ts:9,35-38`), never stored. |
| 2 | `RELEASED` is not a state | **PASS** | `BehaviorState = "CALM"｜"URGENT"｜"CHASING"` (`StateEngine.ts:9`). Completion sets `status="done"` (`TaskRepository.ts:52`). `RELEASING` exists only in the transient *presentation* enum (`CreatureBehavior.ts:146-151`), and `"RELEASED"` only as a debug auto-cycle label (`CreatureDebugTrigger.ts:4`) and a timeline beat name (`DemoSequence.ts:20`) — neither persisted nor part of `BehaviorState`. |
| 3 | Chaser requires `CHASE_THRESHOLD`; 3 fresh → 0 chasers | **PASS** | `StateEngine.isEligibleToChase` (`:31-36`) requires `status==="open" && now >= snoozedUntilMs && urgency >= CHASE_THRESHOLD`. `urgency` = `age/URGENCY_AGE_WINDOW_MS + bonuses` (`:20-27`); fresh ⇒ 0 < 1 ⇒ ineligible ⇒ `selectChaser` returns `null`. Asserted at `Gate2LifecycleScenario.ts:19`. |
| 4 | At most one chaser | **PASS** | `AttentionArbiter.selectChaser` (`:8-21`) keeps a single `selected` and returns one record or `null`. Controller enforces one at presentation level (`TaskOrganismController` per-slot loop, one `requestChase`). |
| 5 | Resolve idempotent; save before effect | **PASS** | `TaskRepository.resolve` (`:49-56`) returns `false` if not found-and-open, so only the first call mutates; `persist()` on `:53` precedes return. `TaskResolutionService.resolve` (`:13-17`) calls `repository.resolve` and only then `releasePresentation`. Additionally `ReleaseEffect` now latches `hasPlayedAudio` for the cue. |
| 6 | Time only via `Clock` | **PASS** | `grep -rn "Date\.now\|getTime()" Assets/Scripts/` → **exactly one hit**: `Data/Clock.ts:8` inside `RealClock.nowMs()`. No other occurrence anywhere. |
| 7 | `schemaVersion` present; parse failure → safe empty | **PASS** | `TaskStorage.ts:19` `TASK_STORAGE_SCHEMA_VERSION = 1`; `load()` returns `[]` on missing key, version mismatch, non-array `tasks`, or `JSON.parse` throw (`:23-33`). |
| 8 | Completed tasks removed from storage | **PASS** | `save()` filters `status === "open"` before writing (`:37-41`); `load()` filters again on read (`:29`); `resolve()` drops done tasks from the in-memory list (`:54`). |

**8 / 8 PASS.** The domain layer is the strongest part of this project.

---

## 4. Tests

### Registered LEAF scenarios (`Assets/Tests/LeafIndex.ts:19-35`)

| ID | Asserts |
|---|---|
| `task-organism-data-layer` | restore / add / snooze / resolve / restart / corrupt-storage recovery |
| `gate2-1-4-chaser-lifecycle` | 3 fresh → 0 chasers; 1 aged → exactly `urgent`; snooze → 0; expiry → 1 |
| `gate2-5-persistence-seed` | seeds a task and persists it |
| `gate2-5-persistence-restore` | separate Lens run restores id + text exactly |
| `gate2-6-8-elapsed-clock` | old task + advanced `DemoClock` selected immediately, no real wait |
| `gate2-7-resolve-idempotency` | double resolve → one save, one release callback |
| `gate3-input-parity` | demo vs keyboard produce identical records |
| `gate3-short-pinch-select` | select only; no repository write, no release |
| `gate3-early-hold-cancel` | early release cancels; selection kept; no write |
| `gate3-completed-hold` | one save + one release despite repeated calls |
| `gate3-no-gesture-conflict` | long first gesture still only selects |
| `gate3-later-snooze` | `Later` sets `snoozedUntilMs`, closes selection |

**Last run: 2026-08-11** (per `prompts.md` Gate 2 / Gate 3 entries). All test
files are dated 2026-08-11 13:32–13:54. **No scenario has been executed since**
— there is no run-record artifact on disk (`leaf_plugin_settings.json` holds
only editor settings), so "currently GREEN" **cannot be claimed today**.

### Are they stale in a way that matters? — measured, and the answer is reassuring

```
git diff --name-only 65215c2..HEAD -- Assets/Scripts/Data Assets/Scripts/State \
                                      Assets/Scripts/Input Assets/Tests
→ (empty)
```
**No Data/, State/, Input/ or Tests/ file changed on 2026-08-12.** And the
domain constants the engine reads are byte-identical:
`CHASE_THRESHOLD 1`, `URGENCY_AGE_WINDOW_MS 86400000`,
`HIGH_IMPORTANCE_URGENCY_BONUS 0.5`, `RESOLVE_HOLD_DURATION_S 1.5`,
`LATER_SNOOZE_DURATION_MS 900000` — all `UNCHANGED` vs `65215c2`.

Also verified: **no scenario references presentation code**
(`grep -ln "CreatureBehavior|habitat|wander|palette|VERTEX" Assets/Tests/*.ts`
→ none). Every scenario is domain-only.

**Conclusion:** the 2026-08-12 presentation work (movement, habitat, scale,
colour, demo sequencing — `CreatureConfig`, `CreatureBehavior`,
`CreaturePetVisual`, `ReleaseEffect`, `DemoControlView`, `DemoSequence`,
`HabitatFloor`, `TaskOrganismController`) **cannot logically have broken any
scenario**. But `TaskOrganismController.ts` did change (+223 lines) and it is
the composition root the scenarios' support files build on, so a re-run is
cheap insurance rather than a formality.

**Scenarios NOT re-run since the 2026-08-12 changes: all 12.**

### Missing tests vs CLAUDE.md "Testing priority"

| Priority | Covered? |
|---|---|
| 1. Arbiter lifecycle 3→0→1→snooze→0→expire→1 | ✅ `gate2-1-4-chaser-lifecycle` |
| 2. Persistence restore across restart | ✅ seed + restore pair |
| 3. Elapsed-time transition without waiting | ✅ `gate2-6-8-elapsed-clock` |
| 4. Resolve idempotency | ✅ `gate2-7-resolve-idempotency` |
| (later) input parity | ✅ `gate3-input-parity` |
| (later) corrupt-storage recovery | ✅ inside `task-organism-data-layer` |

**No gap.** All four priorities plus both "later" items have scenarios.

### TypeScript compile — run today

`RecompileTypeScriptTool` → **`status: "succeeded"`**, no `errors` array.
Runtime refresh (`RunAndCollectLogsTool mode:refresh`) → **zero script errors**;
the only warning is the pre-existing, unrelated
`W (@QNetworkAccessManager thread) stream 3 finished with error: "Host requires authentication"`.

---

## 5. Gate status — honest table

Gate definitions read from `../task-organism-playbook-v3.md` (Russian,
"ВОРОТА"). **Gates 1, 3 and 4 are all defined as human-viewer tests requiring
two independent viewers to pass.**

A search for any viewer evidence
(`grep -rniE "viewer|зрител|showed to|two people|comprehension pass" prompts.md HANDOFF.md`)
returned **zero matches**.

| Gate | Criterion (verbatim intent) | Status | Evidence | Missing |
|---|---|---|---|---|
| **1 — Character Vitality** (Mon) | 10 s clip, no explanation; **2 viewers independently say "alive / cute / creature"** and notice it looking at them | **NOT VALIDATED** | `prompts.md` "Emotional Prototype acceptance" records technical vitality (wander + breathing + glance verified by transform sampling) — no viewer session | A 10 s recording shown to 2 people, answers logged |
| **2 — Product Truth** (Tue) | Manual 4-item checklist: task survives restart; aged task chases; 3 fresh → no chaser; snooze/expire | **PASSED (technically)** | 5 LEAF scenarios returned `succeeded` on 2026-08-11 (`prompts.md` §Gate 2); all 8 invariants re-verified by code reading today | Not re-run since 2026-08-12; hand-check of the 4 items never logged separately |
| **3 — Concept Comprehension** (Wed) | 15–20 s full-cycle clip, no narration, **2–3 viewers**; 2 must understand creature = unfinished task requiring action | **NOT VALIDATED** | `HANDOFF.md:14-16` states it explicitly: *"technically integrated but not human-validated… Do not claim comprehension evidence from the failed 18.7-second recording"* | A clip + 2 viewers. **Was a comprehension recording ever shown to 2+ viewers with a pass result? NO.** |
| **4 — Emotional Payoff** (Thu = today) | Show select → hold → release to **2–3 viewers**; 2 must say the task is done and call the moment pleasant | **NOT STARTED** | Release now has particles + brighten + a 1.1 s audio cue at 74 ms latency, but nothing has been shown to anyone | Recording + 2 viewers. Today's gate. |
| **5 — Submission Safety** (Fri) | Lens in acceptable status; repo opens in incognito; ≥ full day before deadline | **NOT STARTED** | Repo is public (HTTP 200) ✅ — that half holds. Lens publish status unknown | Publish status + incognito clone check |

**Bluntly: the only gate with a defensible pass is Gate 2, and it is a
technical pass. Three of five gates depend on human viewers and zero viewer
sessions have happened.**

---

## 6. CLAD usage (50% of judging score)

### Actually used (from `prompts.md` + this session's history)

| Tool / skill / agent | Uses | To what end |
|---|---|---|
| `RunAndCollectLogsTool` | ~40+ | Every verify cycle; caught the release-cue 510 ms lag, the silent-black shader, reset boundaries |
| `PreviewPanelTool` (screenshot/refresh/getConfig/setConfig) | ~35+ | All visual verification; environment switching |
| `QueryRuntimeSceneTool` | ~20+ | Transform/scale sampling — the tremor audit, grounding, staging verification |
| `RecompileTypeScriptTool` | ~20+ | Compile gate before every runtime check |
| `ExecuteEditorCode` | ~6 | Project identity, auth gate, editor introspection |
| LEAF (install, write, run) | 12 scenarios | The entire domain-truth proof |
| `PreviewInteractTool` | ~8 | Synthetic pinch/poke; found the collider defect |
| `MovePreviewCamera` | ~8 | Framing; **proved** Preview steals hotkeys |
| `InjectPreviewGesture` | ~4 | Key injection; same hotkey proof |
| `ls-clad:lens-studio-router` | 2 | Session entry / gating |
| `ls-clad:specs-experience-builder` | 1 | Calm-idle fix (delegated) |
| `ls-clad:shader-graph` | 1 | Graph edit attempt (**failed**) + codeNode route |
| `ls-clad:build-sfx` | 1 | 3 release variants |
| `QueryLensStudioKnowledgeBase` | 2 | Found `system.getSurfaceColor()` |
| `scene-graphql` / `VirtualScene` | ~3 | Authored-scene reads (day 1–2) |
| `ls-clad:verify-preview` | 1 | Post-build verification convention |

### Not yet used — genuine value vs theatre

| Skill | Judgement |
|---|---|
| **`/specs-publish`** | **GENUINE + REQUIRED.** Playbook §4.4 schedules it for *today*. It is the single largest unknown in the submission. Not optional. |
| **`/specs-capture-perf-trace`** | **GENUINE, cheap.** One trace = a defensible "runs at frame budget on SPECS" claim. ~30 min. |
| **`/specs-lens-perf-attribution`** | **THEATRE at this stage.** A differential sweep is for a project with a perf problem; there is no evidence of one. Skip unless a trace shows trouble. |
| **`/build-mesh`** | **THEATRE now.** Creatures are ready-made GLBs by deliberate decision; regenerating would restart art at day 4. (Note: `/build-mesh` *was* effectively used earlier for `PetCreature.glb`, now dead.) |
| **`/specs-keyboard`** | **THEATRE.** `KeyboardInput` already exists and `gate3-input-parity` passes. Manual deadlines are explicitly out of scope. |
| **`shader-graph`** | Already used (and failed honestly). Re-using it to "fix" `unlit.graphShader` would be **theatre with regression risk** — the codeNode route works. |
| **LEAF skills** | **GENUINE.** Not a new skill, but *re-running* the 12 scenarios is the cheapest credibility win available (~15 min). |
| `/build-music` | Theatre — ambient music is explicitly out of scope in CLAUDE.md. |
| `/specs-optimize-lens-mesh`, `/specs-world-query`, `/specs-camera`, `/specs-ai-remote-service`, `/specs-snap-cloud`, `/specs-sync-kit` | All theatre — out of scope for this Lens. |

### `prompts.md` as a judging exhibit

- **19 cycles**, chronological, with an intro paragraph naming the environment and the full tool list ✅
- Structure per entry: instruction → tools → result (incl. failures) → verification ✅
- Complete **Goal→Prompt→Result→Failure→Diagnosis→Fix→Passed** cycles: **6 strong ones** — additive-display investigation, failed graph-shader edit, URGENT unreachable, pitch-dependent habitat, hotkeys/collider staging failures, cat-tail decimation exoneration. The rest are solid but lighter on the failure/diagnosis arc.
- **Before/after screenshots: NONE.** `grep -nE "!\[|\.png|\.jpg"` on `prompts.md` finds **no image references**, and `find . -name '*.png'` inside the repo returns **nothing committed**. All 47 captures live in the ephemeral session scratchpad (`/private/tmp/claude-501/...`) and **will be lost**.
- Missing for Saturday: embedded before/after images, the agent-handoff cycle has no commit/wall-clock timings, no CLAD-usage summary table at the top, no per-day time accounting (the playbook asks for usage tracking each evening).

---

## 7. Submission deliverables

| Deliverable | Status | Evidence |
|---|---|---|
| `README.md` | **MISSING** | not in root listing; playbook §6.3 schedules it Saturday |
| `eligibility-checklist.md` | **MISSING** | plan §5 says "created Monday, run Friday + before submission" — never created |
| `video-script.md` | **MISSING** | playbook §5.3 schedules it Friday |
| `clips/` | **MISSING** | playbook §4.5 says start filling it *today* |
| Demo video file | **MISSING** | no `.mp4`/`.mov` anywhere (`find . -maxdepth 2`) |
| Exported lens | **MISSING** | no `.lspkg` export found |
| `LICENSES.md` | **PARTIAL** | see below |
| `HANDOFF.md` | EXISTS (109 lines) | current as of 2026-08-11 — **stale**, predates all day-3 work |
| `prompts.md` | EXISTS (267 lines) | current |

### LICENSES.md vs assets actually in the tree

Binary assets on disk: `3d assets/cat.glb`, `3d assets/dog.glb`,
`GeneratedMeshes/PetCreature.glb`, `GeneratedMeshes/cat_lo.glb`,
`GeneratedMeshes/dog_lo.glb`, `GeneratedSFX/Release{Breath,Hum,Bloom}.wav`.

| Asset | Covered? |
|---|---|
| dog.glb / dog_lo.glb | ✅ CC-BY-4.0, author + source + attribution text |
| cat.glb / cat_lo.glb | ✅ CC-BY-4.0, **with an honest flag** that the model title says "Non Commercial", conflicting with the embedded licence — flagged, not resolved |
| `PetCreature.glb` | ❌ **not mentioned** (it is also unreferenced dead payload) |
| 3× Release WAVs | ❌ **not mentioned** — they are algorithmically generated and license-clean by construction, but a judge cannot know that from the file |
| **Accuracy defect** | LICENSES.md states *"No other modification was made to the geometry or textures."* This is now **false**: `Tools/bake-vertex-shading.js` bakes `COLOR_0` into both GLBs and `Tools/reshape-cat-tail.js` moves 100 cat vertices. |

### Publish status

**UNKNOWN.** `grep -rniE "specs-publish|publish|submitted|review"` across
`prompts.md` and `HANDOFF.md` finds no publish record. `/specs-publish` has
**never been run**. What would establish it: run `/specs-publish` (it walks
preflight → content rating → signing key → icon → package → upload → review),
or check the Lens in My Lenses / the Spectacles developer portal. Note
`spk_debug_key.pem` exists locally, which suggests signing was at least
initialised at some point — **UNVERIFIED** whether it was ever used.

---

## 8. Runtime reality check — Lens Studio WAS available

Project confirmed loaded; MCP port 50040 open; PID 40768.

**Logs after refresh:** zero script errors. Startup sequence:
```
[WednesdayDemo] partial save (2/3 open) — reseeding for the demo sequence
[WednesdayEvidence] habitat task=demo-1 slot=1 lateral=-36
[WednesdayEvidence] habitat task=demo-2 slot=2 lateral=0
[WednesdayEvidence] habitat task=demo-3 slot=3 lateral=36
[WednesdayEvidence] beat=CALM t=0.00s open=3
[WednesdayDemo] ready open=3 hold=1.5s chaser=none autoplay=true
```
All 12 LEAF scenarios registered cleanly at startup.

**Live scene:**

| Object | World position |
|---|---|
| `MovementRoot_1` | `(-84.2, -150, -228.8)` enabled |
| `MovementRoot_2` | `(-34.9, -150, -242.0)` enabled |
| `MovementRoot_3` | `(-12.1, -150, -242.4)` enabled |
| `HabitatFloor` | `(-34.6, -150, -171.5)` |
| `Camera Object` | `(0, 0, 0)`; preview camera pose rotation `x 335°, y 11.4°` |

- **3 creature roots**, all enabled.
- **Ground line: y = -150 for all three and the floor disc** — the shared
  `GROUND_Y_OFFSET_CM` holds; creatures and floor agree exactly. ✅
- **Habitat depth ≈ 2.29–2.42 m** — consistent with `HABITAT_HOME_DEPTH_CM 240`.
  Lateral x-values are not the nominal −36/0/+36 because the preview camera
  carries **11.4° of yaw**, which rotates the habitat basis. Expected, not a bug.
- **Demo sequence autoplays: YES** (`autoplay=true`, `beat=CALM t=0.00s`).

**Preview capture** (`scratchpad/audit/state.png`): Evening Room, camera pitched
down ~25° and yawed 11°. Visible: the green cat and orange dog standing on the
carpet with contact shadows, correct grounding, task labels above each, the
6-button staging panel on the left, and the status plate reading
`2 tasks remaining` / `Time advanced` — i.e. the scripted story had already run
to completion at capture time. The third creature sits off-frame left.
The staging panel occupies roughly the left third of the view.

**Additive-display constraint:** still applies — it is display physics, not a
setting, so nothing in the day-3 work could remove it. Current saturated palette
(amber / green / orange) reads cleanly against this dark room.
**UNVERIFIED against a bright backdrop today** — would require switching the
Interactive environment to a bright scene and re-capturing.

---

## 9. Risk register and time budget

### Risks, ranked by expected damage

| # | Risk | Likelihood | Impact | Cheapest mitigation |
|---|---|---|---|---|
| 1 | **Publish path never exercised**; discovers a blocker (content rating, signing, review latency) on Sunday | HIGH | FATAL | Run `/specs-publish` **today**. Even reaching phase 3 de-risks it. |
| 2 | **Three gates never human-validated**; submission claims comprehension it cannot evidence | CERTAIN today | HIGH | One 20 s recording + 2 viewers covers Gates 3 and 4 together. ~45 min. |
| 3 | **No video, no clips, no script** with 3.5 days left | HIGH | FATAL | Start `clips/` today per playbook §4.5; script Friday |
| 4 | **No README / eligibility checklist** | HIGH | HIGH | Both are ~1 h of writing; eligibility was due Monday |
| 5 | **prompts.md has no images**, captures are in ephemeral `/private/tmp` | CERTAIN | MEDIUM-HIGH (50% of score is CLAD) | Copy the best 6–8 captures into `docs/` and reference them. ~30 min. |
| 6 | LICENSES.md contains a **false statement** about geometry modification | CERTAIN | MEDIUM (licence accuracy) | 10-minute edit |
| 7 | LEAF scenarios not re-run since day 3 | CERTAIN | MEDIUM | Re-run all 12 in Preview, ~15 min |
| 8 | LFS repo may not clone cleanly for judges | UNVERIFIED | HIGH if true | Incognito clone test, ~10 min |
| 9 | Chase side-offset is 0° vs CLAUDE.md's 8–12° | CERTAIN | LOW-MEDIUM | One constant; verify it still reads well |
| 10 | Staging panel occupies ⅓ of frame in recordings | CERTAIN | MEDIUM (video quality) | Add a hide toggle, or frame around it |
| 11 | Dead code + unused 1.4 MB asset in a judged repo | CERTAIN | LOW | Delete after freeze, not before |

### Work breakdown

**P0 — submission-blocking (~13.5 h)**
| Item | Est. |
|---|---|
| `/specs-publish` first candidate, all phases | 2.0 h |
| Record Gate 3 + Gate 4 material, show to 2 viewers, log results | 1.5 h |
| Collect clips (habitat, notice, chase, release close-up) | 1.0 h |
| Demo video edit to 55 s per plan §4 | 3.0 h |
| `README.md` | 1.0 h |
| `eligibility-checklist.md` + run it | 1.0 h |
| `video-script.md` | 1.0 h |
| prompts.md final: embed images, add usage table | 1.5 h |
| Re-run 12 LEAF scenarios, record result | 0.5 h |
| Incognito clone + LFS verification | 0.25 h |
| LICENSES.md correction + WAV/PetCreature coverage | 0.25 h |
| Update stale `HANDOFF.md` | 0.5 h |

**P1 — materially improves the score (~3 h)**
`/specs-capture-perf-trace` (0.5 h) · staging-panel hide toggle for recording
(0.5 h) · chase side-offset restored to 8–12° and re-verified (0.5 h) · Gate 1
vitality clip retro-validated with the same viewers (0.5 h) · pick the SFX
variant and commit it (0.25 h) · per-day time accounting in the log (0.75 h)

**Cuttable**
Dead-code removal · `PetCreature.glb` deletion · perf attribution sweep ·
any new feature · re-fixing `unlit.graphShader` · cat-tail refinement ·
additive-display re-test against a bright backdrop

### Against ~3.5 days

P0 is ~13.5 h; at the plan's 6 h/day that is ~2.25 days of the ~3.5 available.
It fits **only if feature work stops now**. The playbook's FEATURE FREEZE was
scheduled for today and should be honoured literally. **Everything in P1 is
cuttable if publish or the recordings go badly.**

### Three most likely ways this fails

1. **Publish blocks on Sunday** — a rating, signing or review-latency
   requirement discovered too late to satisfy.
2. **No watchable video** — clips never collected, so Saturday's edit has no
   footage and the submission ships a static repo.
3. **CLAD score underperforms** — 50% of judging, and the exhibit is a
   text-only log with no images, no usage table and no time accounting, while
   the strongest material (honest failures) sits un-illustrated.

### Single next action that most reduces total risk

**Run `/specs-publish` today and record exactly where it stops.** It is the
only unknown that can be fatal and cannot be compressed later; everything else
on the P0 list is work whose duration is already known.

---

## 10. Open questions for the human

1. **Have you shown *any* recording to viewers yet?** No trace exists in the log
   — if sessions happened, they need recording; if not, Gates 1/3/4 stay NOT VALIDATED.
2. **Who are the 2–3 viewers, and when are they available?** Gates 3 and 4 both
   need them; one sitting can cover both.
3. **Snapchat / Spectacles developer account state** — is the account able to
   publish, and has `spk_debug_key.pem` ever been used for a real submission?
4. **Which release SFX variant** (`breath` / `hum` / `bloom`)? Config currently
   holds `breath` as a capture default, explicitly not a choice.
5. **Claude/Codex usage budget remaining** — the playbook asks for a nightly
   usage note; none recorded since day 1.
6. **Is the "Bengal Cat Non Commercial" licence conflict acceptable** for a
   public hackathon submission, or should the cat be swapped?
7. **Do you want the staging panel hidden** in the final recording, or is it
   acceptable as a visible dev control?

---

## Verdict

**Behind — by roughly one full day, and the gap is entirely in validation and
packaging, not in code.** The engineering is genuinely strong: 8/8 invariants
verified by reading source today, all four CLAUDE.md testing priorities covered
by 12 LEAF scenarios, a clean TypeScript compile, zero runtime errors, a shared
ground reference that holds exactly at y=-150 across creatures and floor, and a
day-3 presentation pass that measurably fixed drift, tremor, flatness and cue
timing. Against that, the calendar says Thursday and the project has not
completed a single *human* gate, has never run the publish path that the plan
scheduled for today, has no clips, no video, no README, no eligibility
checklist, and zero commits today. The frozen playbook put FEATURE FREEZE at
this point precisely so the last three days could be spent on packaging; the
build is ready for that freeze and has not taken it. If publish is exercised
today and one viewer session covers Gates 3 and 4, the submission lands with
room to spare — if publish slips past Friday, the most likely outcome is a
technically excellent Lens that cannot be submitted in an acceptable state.
